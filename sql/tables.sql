CREATE TABLE USERS 
(
	USER_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	USERNAME VARCHAR(50) NOT NULL,
	NORMALIZED_USERNAME VARCHAR(50) DEFAULT '' NOT NULL,
	VORNAME VARCHAR(50) DEFAULT '' NOT NULL,
	NACHNAME VARCHAR(50) DEFAULT '' NOT NULL,
	ANZEIGENAME VARCHAR(100) DEFAULT '' NOT NULL,
	ACTIVE_DIRECTORY_GUID VARCHAR(36),
	EMAIL VARCHAR(255) DEFAULT '' NOT NULL,
	PASSWORD VARCHAR(255) DEFAULT '' NOT NULL,
	SALT VARCHAR(255) DEFAULT '' NOT NULL,
	ACCOUNT_TYPE VARCHAR(20) DEFAULT 'LocalAccount' NOT NULL,
	IS_AKTIV BOOLEAN DEFAULT FALSE NOT NULL,
	IS_ADMIN BOOLEAN DEFAULT FALSE NOT NULL,
	CONSTRAINT USERS_PK PRIMARY KEY (USER_ID)
);

CREATE TABLE ABTEILUNGEN 
(
	ABTEILUNG_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(50) NOT NULL,
	ACTIVE_DIRECTORY_GROUP_CN VARCHAR(255) DEFAULT '' NOT NULL,
	CONSTRAINT ABTEILUNGEN_PK PRIMARY KEY (ABTEILUNG_ID)
);

CREATE TABLE ERP_CONFIG 
(
	CONFIG_KEY VARCHAR(100) NOT NULL,
	CONFIG_VALUE VARCHAR(255) NOT NULL,
	CONSTRAINT CONFIG_PK PRIMARY KEY (CONFIG_KEY)
);

CREATE TABLE LAENDER 
(
	LAND_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(30) NOT NULL,
	ISO2 VARCHAR(2) NOT NULL,
	ISO3 VARCHAR(3) NOT NULL,
	VORWAHL VARCHAR(5) DEFAULT '' NOT NULL,
	IST_EU BOOLEAN DEFAULT FALSE NOT NULL,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_LANDESKENNZEICHEN PRIMARY KEY (LAND_ID),
	CONSTRAINT UK_LANDESKENNZEICHEN_ISO2 UNIQUE (ISO2),
	CONSTRAINT UK_LANDESKENNZEICHEN_ISO3 UNIQUE (ISO3),
    CONSTRAINT FK_LANDESKENNZEICHEN_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);

CREATE TABLE SPRACHEN 
(
	SPRACH_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(30) NOT NULL,
	CONSTRAINT SPRACHEN_PK PRIMARY KEY (SPRACH_ID)
);

CREATE TABLE UEBERSETZUNGEN 
(
	CODE VARCHAR(50) NOT NULL,
	SPRACH_ID INTEGER NOT NULL,
	PARENT_ID INTEGER NOT NULL,
	VALUE_TEXT VARCHAR(255),
	VALUE_BLOB BLOB SUB_TYPE TEXT,
	CONSTRAINT UEBERSETZUNGEN_PK PRIMARY KEY (CODE,PARENT_ID,SPRACH_ID),
	CONSTRAINT UEBERSETZUNGEN_FK_SPRACH_ID FOREIGN KEY (SPRACH_ID) REFERENCES SPRACHEN(SPRACH_ID)
);

CREATE TABLE ANREDEN 
(
	ANREDE_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(50),
    LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_ANREDEN PRIMARY KEY (ANREDE_ID),
    CONSTRAINT FK_ANREDEN_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);

CREATE TABLE WAEHRUNG 
(
	CODE VARCHAR(3) NOT NULL,
	KURS DECIMAL(15,8),
	WECHSELKURS DECIMAL(15,8),
	MUSS_RUNDEN BOOLEAN DEFAULT FALSE NOT NULL,
	NACHKOMMASTELLEN INTEGER DEFAULT 0,
	KURS_VOM DATE,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_WAEHRUNG PRIMARY KEY (CODE),
	CONSTRAINT FK_WAEHRUNG_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);

CREATE TABLE KOSTENSTELLEN
(
	NUMMER INTEGER NOT NULL,
	NAME VARCHAR(100) DEFAULT '' NOT NULL,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_KOSTENSTELLEN PRIMARY KEY (NUMMER),
	CONSTRAINT FK_KOSTENSTELLEN_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);


CREATE TABLE ZAHLUNGSBEDINGUNGEN
(
	ZAHLUNGSBEDINGUNG_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(50) DEFAULT '' NOT NULL,
	NETTOTAGE INTEGER DEFAULT 0 NOT NULL,
	SKONTO1_TAGE INTEGER DEFAULT 0 NOT NULL,
	SKONTO1_PROZENT NUMERIC(5,2) DEFAULT 0 NOT NULL,
	SKONTO2_TAGE INTEGER DEFAULT 0 NOT NULL,
	SKONTO2_PROZENT NUMERIC(5,2) DEFAULT 0 NOT NULL,
	IST_VORKASSE BOOLEAN DEFAULT FALSE NOT NULL,
	IST_BARZAHLUNG BOOLEAN DEFAULT FALSE NOT NULL,
	IST_ABBUCHUNG BOOLEAN DEFAULT FALSE NOT NULL,
	IST_RECHNUNG BOOLEAN DEFAULT FALSE NOT NULL,
	IST_AKTIV BOOLEAN DEFAULT FALSE NOT NULL,
	VERFUEGBAR_KUNDE BOOLEAN DEFAULT FALSE NOT NULL,
	VERFUEGBAR_LIEFERANT BOOLEAN DEFAULT FALSE NOT NULL,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_ZAHLUNGSBEDINGUNGEN PRIMARY KEY (ZAHLUNGSBEDINGUNG_ID),
	CONSTRAINT FK_ZAHLUNGSBEDINGUNGEN_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);

CREATE TABLE LIEFERBEDINGUNGEN 
(
	LIEFERBEDINGUNG_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(50) DEFAULT '' NOT NULL,
	VERSAND_MIT_SPEDITION BOOLEAN DEFAULT FALSE NOT NULL,
	IST_ABHOLUNG BOOLEAN DEFAULT FALSE NOT NULL,
	VERFUEGBAR_KUNDE BOOLEAN DEFAULT FALSE NOT NULL,
	VERFUEGBAR_LIEFERANT BOOLEAN DEFAULT FALSE NOT NULL,
	IST_AKTIV BOOLEAN DEFAULT FALSE NOT NULL,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_LIEFERBEDINGUNGEN PRIMARY KEY (LIEFERBEDINGUNG_ID),
	CONSTRAINT FK_LIEFERBEDINGUNGEN_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);


CREATE TABLE VOREINSTELLUNGEN
(
	VOREINSTELLUNG_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(50) NOT NULL,
	KUNDE_LIEFERBEDINGUNG_ID INTEGER,
	KUNDE_ZAHLUNGSBEDINGUNG_ID INTEGER,
	KUNDE_ANREDE_ID INTEGER,
	KUNDE_WAEHRUNGSCODE VARCHAR(3),
	KUNDE_LAND_ID INTEGER,
	KUNDE_SPRACH_ID INTEGER,
	KUNDE_KREDITLIMIT DECIMAL(15,2) DEFAULT 0 NOT NULL,
	KUNDE_NEUTRALER_VERSAND BOOLEAN DEFAULT FALSE NOT NULL,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_VOREINSTELLUNGEN PRIMARY KEY (VOREINSTELLUNG_ID),
	CONSTRAINT FK_VOREINSTELLUNGEN_LIEFERBEDINGUNG FOREIGN KEY (KUNDE_LIEFERBEDINGUNG_ID) REFERENCES LIEFERBEDINGUNGEN(LIEFERBEDINGUNG_ID) ON UPDATE CASCADE ON DELETE SET NULL,
	CONSTRAINT FK_VOREINSTELLUNGEN_ZAHLUNGSBEDINGUNG FOREIGN KEY (KUNDE_ZAHLUNGSBEDINGUNG_ID) REFERENCES ZAHLUNGSBEDINGUNGEN(ZAHLUNGSBEDINGUNG_ID) ON UPDATE CASCADE ON DELETE SET NULL,
	CONSTRAINT FK_VOREINSTELLUNGEN_ANREDE FOREIGN KEY (KUNDE_ANREDE_ID) REFERENCES ANREDEN(ANREDE_ID) ON UPDATE CASCADE ON DELETE SET NULL,
	CONSTRAINT FK_VOREINSTELLUNGEN_WAEHRUNG FOREIGN KEY (KUNDE_WAEHRUNGSCODE) REFERENCES WAEHRUNG(CODE) ON UPDATE CASCADE ON DELETE SET NULL,
	CONSTRAINT FK_VOREINSTELLUNGEN_LAND FOREIGN KEY (KUNDE_LAND_ID) REFERENCES LAENDER(LAND_ID) ON UPDATE CASCADE ON DELETE SET NULL,
	CONSTRAINT FK_VOREINSTELLUNGEN_SPRACHE FOREIGN KEY (KUNDE_SPRACH_ID) REFERENCES SPRACHEN(SPRACH_ID) ON UPDATE CASCADE ON DELETE SET NULL,
	CONSTRAINT FK_VOREINSTELLUNGEN_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);

CREATE TABLE NUMMERNKREISE
(
	NUMMERNKREIS_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	NAME VARCHAR(50) NOT NULL,
	KUNDE_VON INTEGER,
	KUNDE_BIS INTEGER,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	PRIMARY KEY (NUMMERNKREIS_ID),
	CONSTRAINT FK_NUMMERNKREISE_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);

CREATE TABLE KUNDEN
(
	KUNDENNUMMER VARCHAR(10) NOT NULL,
	FIRMA VARCHAR(60) DEFAULT '',
	NAME1 VARCHAR(60) DEFAULT '',
	NAME2 VARCHAR(60) DEFAULT '',
	ANLAGEDATUM DATE DEFAULT CURRENT_TIMESTAMP NOT NULL,
	SPRACH_ID INTEGER NOT NULL,
	LAND_ID INTEGER NOT NULL,
	STRASSE VARCHAR(50) NOT NULL,
	POSTLEITZAHL VARCHAR(8) NOT NULL,
	ORT VARCHAR(40) NOT NULL,
	TELEFONNUMMER VARCHAR(30) DEFAULT '' NOT NULL,
	MOBILNUMMER VARCHAR(30) DEFAULT '' NOT NULL,
	FAXNUMMER VARCHAR(30) DEFAULT '' NOT NULL,
	EMAIL VARCHAR(255) DEFAULT '' NOT NULL,
	WEBSITE VARCHAR(60) DEFAULT '' NOT NULL,
	NOTIZ BLOB SUB_TYPE TEXT DEFAULT '' NOT NULL,
	ANREDE_ID INTEGER,
	ZAHLUNGSBEDINGUNG_ID INTEGER NOT NULL,
	LIEFERBEDINGUNG_ID INTEGER NOT NULL,
	UMSATZSTEUER_IDENTIFIKATIONSNUMMER VARCHAR(30) DEFAULT '' NOT NULL,
	KREDITLIMIT DECIMAL(15, 2) DEFAULT 0 NOT NULL,
	IBAN VARCHAR(40) DEFAULT '' NOT NULL,
	BIC VARCHAR(15) DEFAULT '' NOT NULL,
	IST_GESPERRT BOOLEAN DEFAULT FALSE NOT NULL,
	NEUTRALER_VERSAND BOOLEAN DEFAULT FALSE NOT NULL,
	WAEHRUNGSCODE VARCHAR(3) NOT NULL,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT PK_KUNDEN PRIMARY KEY (KUNDENNUMMER),
	CONSTRAINT FK_KUNDEN_USER FOREIGN KEY (LETZTER_BEARBEITER) REFERENCES USERS(USER_ID) ON UPDATE CASCADE ON DELETE SET NULL
);

CREATE TABLE KUNDEN_LIEFERANSCHRIFTEN
(
	LIEFERANSCHRIFT_ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	KUNDENNUMMER VARCHAR(10) NOT NULL,
	ANSCHRIFT_NR INTEGER NOT NULL,
	FIRMA VARCHAR(60) DEFAULT '' NOT NULL,
	NAME1 VARCHAR(60) DEFAULT '' NOT NULL,
	NAME2 VARCHAR(60) DEFAULT '' NOT NULL,
	STRASSE VARCHAR(50) DEFAULT '' NOT NULL,
	LAND_ID INTEGER NOT NULL,
	POSTLEISTZAHL VARCHAR(8) DEFAULT '' NOT NULL,
	ORT VARCHAR(40) DEFAULT '' NOT NULL,
	TELEFONNUMMER VARCHAR(30) DEFAULT '' NOT NULL,
	MOBILNUMMER VARCHAR(30) DEFAULT '' NOT NULL,
	FAXNUMMER VARCHAR(30) DEFAULT '' NOT NULL,
	EMAIL VARCHAR(255) DEFAULT '' NOT NULL,
	NOTIZ BLOB SUB_TYPE TEXT DEFAULT '' NOT NULL,
	LETZTER_BEARBEITER INTEGER DEFAULT NULL,
    ZULETZT_GEAENDERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	PRIMARY KEY (LIEFERANSCHRIFT_ID)
);
